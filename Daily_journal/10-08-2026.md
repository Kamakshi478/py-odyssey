# Question solving 
# topic - Reversing the digit 
Given a signed 32-bit integer x, return x with its digits reversed. If reversing x causes the value to go outside the signed 32-bit integer range [-231, 231 - 1], then return 0.

Assume the environment does not allow you to store 64-bit integers (signed or unsigned).

 Example 1:

Input: x = 123
Output: 321
Example 2:

Input: x = -123
Output: -321
Example 3:

Input: x = 120
Output: 21
 

Constraints:

-231 <= x <= 231 - 1

## Question Requirements
  -  Reversing the digit x.
  -  If reversing the digit x does not comes under the signed 32 bit integer range then the output  will be 0.
 ## constraints 
   - x will be from -2^31 to 2^31-1.
##  Output 
  - Reversed X value.
print(f"{x=}") 
  -  1234 -> Swap 1 and 4 , swap 2 and 3 -> 4321
  -  12345 -> swap 1 and 5 ,swap 2 and 4 -> 54321
  -  123456 -> swap 1 and 6 , swap 2 and 5 , swap 3 and 4 -> 654321
    #  convert integer to a string 
        _str = str(x)
        print(f"{x_str=}")
        x_list = list(x_str)
        print(f"{x_list=}")
        # 4 -> 2, 5 -> 2, 6 -> 3, 7 -> 3,8 -> 4 
        n = len(x_list)
        print(f"{n=}")  

   for i in range( int((n-1)/2)  if n%2 == 1 else int(n/2) ):
            print(f"{i=}")
            print(f"{ x_list[i]=}")
            # 1- i=0 , 6- n-1-i=5 , n= 6
            # 2- i=1 , 5- n-1-i=4 , n= 6
            # 3- i=2 , 4- n-1-i=3 , n= 6
    
